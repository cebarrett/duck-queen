/// <reference types="vite/client" />
// This one line tells TypeScript about Vite's special imports — like
// `import './style.css'` and importing image/`.glb` files as URLs — so the
// type-checker stops complaining about them. (We'll lean on this when we load
// real models later.)
