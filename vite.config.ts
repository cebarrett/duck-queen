import { defineConfig } from 'vite'

// Vite needs almost no config for this project. `open: true` just pops the
// browser tab when you run `npm run dev`.
export default defineConfig({
  server: {
    open: true,
  },
})
